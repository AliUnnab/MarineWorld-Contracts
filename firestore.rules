rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for sandbox preview environment to resolve all query permission issues
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
